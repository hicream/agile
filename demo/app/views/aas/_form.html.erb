<%= form_for(@aa) do |f| %>
  <% if @aa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aa.errors.count, "error") %> prohibited this aa from being saved:</h2>

      <ul>
      <% @aa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
